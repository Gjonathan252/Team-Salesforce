<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>All Reservations</h1>
    <%reservations.forEach(reservation => {%>
        <p><%= reservation.user_name %></p>
        <p><%= reservation.party_size %></p>
        <p><%= reservation.table_num %></p>
        <p><%= reservation.start_date %></p>
        <a href="/reservations/delete/<%= reservation._id %>" class="item__delete-text">Delete Reservation/Complete Reservation</a>
        <p>End of one reservation</p>
        <%})%>

    <h1>All Orders</h1>
    <%orders.forEach(order => {%>
        <p><%= order.customer_name %></p>
        <p><%= order.date %></p>
        <p><%= order.customer_contact %></p>
        <p><%= order.item_list %></p>
        <p><%= order.total_price %></p>
        <a href="/reservations/remove/<%= order._id %>" class="item__delete-text">Delete Order/Complete Order</a>
        <%})%>
</body>
</html>